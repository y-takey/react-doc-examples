import { PlayGround, Knobs, PropsTable } from 'react-display-window/lib/components';
import "antd/dist/antd.css";

import PrimaryButton from "./PrimaryButtonNoFlow";

# Button

プライマリーボタン


## Example

This will render the component and the code.

<PlayGround>
  <PrimaryButton text="hoge" onClick={() => console.log("clicked!")} />
</PlayGround>

## Props

### PropsTable

react-display-window don't support flow-type maybe.
